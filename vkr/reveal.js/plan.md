1. Титульный слайд
Тема, ФИО, группа, руководитель.

2. Актуальность
Почему проблема важна.

3. Цель и задачи
Описать цель и задачи.

4. Методика верификации
Рис. общей методики (предоставлю). Описание методики

5.1 Математическая постановка задачи

Математическая постановка задачи заключается в нахождении такого значения
$k \in \mathbb{N}_{0}$, для которого показатель качества согласованности $q$
максимален при ограничениях.
Иначе говоря, требуется решить задачу оптимизации
\begin{equation}\label{f13}
\left\{
\begin{aligned}
&q(k) \to \max\limits_{k},\\
&k \le m,\\
&k \in K_{\mathrm{доп}}(\mathcal{M},\tau).
\end{aligned}
\right.
\end{equation}

5.2. Решение поставленной задачи.

\begin{algorithm}[htp!]
    \SetAlgoLined
    \KwData{система переходов $\mathcal{M} = (X, X_0, R)$; трасса $\tau = (\sigma_1,\dots,\sigma_m) \in \Sigma^m$; предикат согласованности $C : \Sigma \times X \times X \to \{0,1\}$}
    \KwResult{значение $k^*(\mathcal{M},\tau)$, значение $Q(\mathcal{M},\tau)$ и ответ о полной совместимости трассы}

    $D_0 \gets X_0$\;
    $k^* \gets 0$\;

    \ForEach{$i \in \{1,\dots,m\}$}{
        $D_i \gets \left\{
            y \in X
            :
            \exists x \in D_{i-1} : (x,y) \in R \land C(\sigma_i,x,y)=1
        \right\}$\;

        \If{$D_i = \varnothing$}{
            \textbf{прервать цикл}\;
        }

        $k^* \gets i$\;
    }

    $Q(\mathcal{M},\tau) \gets \frac{k^*}{m}$\;

    \If{$k^* = m$}{
        \Return{$k^*$, $Q(\mathcal{M},\tau)$, $\tau$ полностью совместима с $\mathcal{M}$}\;
    }
    \Return{$k^*$, $Q(\mathcal{M},\tau)$, $\tau$ не полностью совместима с $\mathcal{M}$}\;
    \caption{Нахождение максимального согласованного префикса трассы}
    \label{alg:trace-quality}
\end{algorithm}

6.1 Методика верификации (возврат к слайду)
Подсвечиваем на рис часть про реализацию, подводим к РВС.

6.2 Система распределённых вычислений
Клиент, кластер Raft, вычислительные узлы.

7.1 Методика верификации (возврат к слайду)
Подсвечиваем на рис часть про спецификацию, подводим.

7.2 TLA+ спецификация алгоритма Raft
Что формализовано и зачем это нужно дальше.

8.1 Методика верификации (возврат к слайду)
Подсвечиваем на рис часть про трассы, подводим к Библиотека инструментирования и формат трассы.

8.2 Библиотека инструментирования и формат трассы
Как формируются записи трассы, пример трассы.

9.1 Методика верификации (возврат к слайду)
Подсвечиваем на рис часть про спецификации трассы. Подводим к интеграции.

9.2 Как оно работает

C = Имя события, параметры, обновления переменных.
Как математическая постановка переводится в TLA+.

10. Тестирование системы верификации
Какая ошибка была найдена и почему метод её обнаружил.

11. Организационно-экономическая часть

Кратко: трудозатраты, стоимость, целесообразность.

12. Организационно-правовое обеспечение ИБ

Ключевые НПА и вывод.

13. Заключение и перспективы

Итоги, Lua, Tarantool.
